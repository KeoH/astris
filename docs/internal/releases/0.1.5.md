# Astris 0.1.5

Release date: 2026-03-01

## Highlights

- Maintenance release focused on version alignment and release documentation readiness.

## What's changed

### Added
- Added internal release notes for `0.1.5` at `docs/internal/releases/0.1.5.md`.

### Changed
- Updated package version metadata to `0.1.5` in project metadata and runtime exports.
- Updated changelog with the `0.1.5` release entry.

### Fixed
- Aligned release metadata across repository files to keep packaging and release automation consistent.

## Why this matters

- Ensures a clean and consistent release state before publishing artifacts.
- Reduces packaging/release friction caused by mismatched version metadata.

## Validation

- `uv run --group dev pyright` -> `0 errors, 0 warnings, 0 informations`
- `uv run --group dev pytest` -> `39 passed`

## Upgrade notes

No migration is required.

## GitHub release body (copy/paste)

Astris 0.1.5 is a maintenance release that aligns package version metadata and release documentation.

### Added
- Internal release notes for `0.1.5` in `docs/internal/releases/0.1.5.md`.

### Changed
- Version bump to `0.1.5` across package metadata and runtime export.
- Changelog updated with the `0.1.5` entry.

### Fixed
- Release metadata alignment across repository files for a predictable packaging workflow.
